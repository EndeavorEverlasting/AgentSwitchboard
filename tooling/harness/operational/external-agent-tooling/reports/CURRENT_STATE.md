# External Agent Tooling — Current State

**Status:** TRACKED / OFFLINE CONTRACT PROVEN WHEN VALIDATORS PASS
**Captured:** 2026-08-16
**Catalog:** `agentswitchboard.external-agent-tooling.v1`

## Working

- 45 supplied or materially referenced technologies/skill surfaces are represented in one machine-readable registry.
- Every entry carries provenance bucket, evidence state, disposition, and a mandatory pre-adoption verification flag.
- Quantitative DeepSeek Harness savings language is isolated as a reported claim and cannot be reused as verified evidence.
- The supplied `Ader` spelling is preserved as an alias while the catalog uses the resolved public name `Aider`.
- Existing-reference routes are distinguished from evaluation candidates so AgentSwitchboard can reuse current OpenCode, Hermes, and Goose surfaces rather than fork ownership.
- A deterministic intake workflow and scoped skill require upstream/version/security/privacy/rollback evidence before adoption.
- Portable Python and PowerShell validators protect completeness and anti-install-authority invariants.

## Unproven / intentionally not claimed

- No third-party package was installed or executed by this sprint.
- No provider credentials, provider calls, model downloads, network endpoints, telemetry behavior, performance claims, benchmark claims, privacy claims, or live integrations were proven.
- The exact source identity for `Anti-gravity` and `Cognite` remains unresolved in this catalog.
- Video-to-project linkage for Aider and all detailed video claims still requires source-level verification before adoption.

## Missing

Each `evaluate` candidate still needs its own bounded upstream-verification/evaluation run before a repository integration decision. The catalog is the routing floor for those future sprints, not evidence that they should all be adopted.

## Proof ceiling

Tracked catalog structure, completeness, provenance/evidence labels, dispositions, workflow/skill routing, and offline/static validator behavior only.
