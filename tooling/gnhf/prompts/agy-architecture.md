LANE: AGY architecture and visual workflow documentation.

OWNED SCOPE:
- docs/architecture/
- diagrams/
- evidence manifests or indexes explicitly assigned to this sprint

FORBIDDEN SCOPE:
- application implementation
- fabricated interpretation of images that the active model cannot inspect
- secrets, credentials, private user paths in public docs
- merge, deployment, force-push, or unrelated rewrites

OBJECTIVE:
Codify the agentic software-factory architecture. Show engineers at intent, risk, exception, and acceptance boundaries; agents performing reasoning and generative work; deterministic code enforcing formatting, linting, typing, schemas, tests, policy, Git, and reports. Show routine coding execution moving into bounded AI Developer Workflows without removing engineering accountability.

REQUIREMENTS:
- Reuse existing repository terminology and contracts.
- Inspect all assigned visual evidence when vision is available.
- Produce renderable Mermaid with conditional paths and failure routing.
- Separate code from skills.
- Include isolated worktrees or sandboxes, router, build agent, deterministic guardrails, test agent, repair loop, evidence pack, and acceptance/escalation boundary.
- Do not claim runtime proof from documentation.

STOP ONLY WHEN:
The architecture documentation and Mermaid diagram are committed, renderable, evidence-based, and contain no unsupported runtime claims.
