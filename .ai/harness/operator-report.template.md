# Repository Family Harness Status

## Run

- run ID: `{{runId}}`
- generated UTC: `{{generatedUtc}}`
- proof level: `read-only-repository-intake`
- proof ceiling: local clone identity, Git state, and required-path readiness only

## Summary

- total repositories: `{{total}}`
- ready: `{{ready}}`
- partial: `{{partial}}`
- blocked: `{{blocked}}`
- not present: `{{notPresent}}`

## Repository status

For each registered repository report:

- repository and role;
- status: ready, partial, blocked, or not present;
- local directory name only, never a full user path;
- branch, HEAD, and dirty state when available;
- missing required harness paths;
- declared validator commands;
- exact next command;
- proof ceiling.

## Safety statement

This report is generated by a read-only local probe. It does not clone, fetch, update, switch, clean, validate, launch an agent, access a provider, mutate a target, merge, deploy, or promote child-repository proof.

## Handoff

The receiving agent must re-inspect Git and repository-local harness state before acting. A prior `ready` observation is not continuing authority.
