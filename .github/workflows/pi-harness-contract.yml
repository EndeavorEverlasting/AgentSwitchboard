name: Pi operational harness contract

on:
  pull_request:
    paths:
      - "tooling/pi/**"
      - "Bootstrap-Pi-SystemWide.cmd"
      - "Pull-And-Run-AgentSwitchboard.cmd"
      - "tooling/profiles/windows/Setup-TechnicianAgentSwitchboard.ps1"
      - ".ai/skills/pi-fusion-orchestration/**"
      - "scripts/Test-PiHarnessCompleteness.ps1"
      - "tests/test_pi_harness_contracts.py"
      - "tests/test_pi_system_bootstrap.py"
      - "tests/Test-PiWorkstationPrereqsContracts.ps1"
      - "docs/harness/pi-operational-harness.md"
      - "docs/harness/pi-system-bootstrap-and-child-agents.md"
      - ".ai/harness/manifest.json"
      - ".ai/harness/artifact-registry.json"
      - "CODEBASE_MAP.md"
      - "SKILLS.md"
      - "CAPABILITIES.md"
      - "TRIGGERS.md"
      - ".github/workflows/pi-harness-contract.yml"
  push:
    paths:
      - "tooling/pi/**"
      - "Bootstrap-Pi-SystemWide.cmd"
      - "Pull-And-Run-AgentSwitchboard.cmd"
      - "tooling/profiles/windows/Setup-TechnicianAgentSwitchboard.ps1"
      - "scripts/Test-PiHarnessCompleteness.ps1"
      - "tests/test_pi_harness_contracts.py"
      - "tests/test_pi_system_bootstrap.py"
      - "tests/Test-PiWorkstationPrereqsContracts.ps1"
      - "docs/harness/pi-operational-harness.md"
      - "docs/harness/pi-system-bootstrap-and-child-agents.md"
      - ".github/workflows/pi-harness-contract.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux-contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run Pi workstation npm compatibility report-only path
        shell: pwsh
        run: ./tooling/pi/Test-PiWorkstationPrereqs.ps1 -NoNetwork -NoWrite -AllowUnready
      - name: Run executable Pi workstation prerequisite contracts
        shell: pwsh
        run: ./tests/Test-PiWorkstationPrereqsContracts.ps1
      - name: Run dependency-free Pi harness contracts
        run: python tests/test_pi_harness_contracts.py
      - name: Run dependency-free Pi system and child contracts
        run: python -m unittest tests.test_pi_system_bootstrap -v
      - name: Run Pi completeness contract
        shell: pwsh
        run: ./scripts/Test-PiHarnessCompleteness.ps1
      - name: Run Pi read-only status
        shell: pwsh
        run: ./tooling/pi/Get-PiHarnessStatus.ps1 -NoWrite
      - name: Run canonical documentation contract
        shell: pwsh
        run: ./scripts/Test-AgentDocumentationContract.ps1
      - name: Full PR-range hygiene
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --check "origin/${GITHUB_BASE_REF}...HEAD"
          test -z "$(git status --short)"
      - name: Push-range hygiene
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git diff --check HEAD^..HEAD
          test -z "$(git status --short)"

  windows-contracts:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Parse Pi and dispatcher PowerShell surfaces
        shell: pwsh
        run: |
          $failures = [System.Collections.Generic.List[string]]::new()
          foreach ($path in @(
            'scripts/Test-PiHarnessCompleteness.ps1',
            'tooling/pi/Test-PiWorkstationPrereqs.ps1',
            'tooling/pi/Get-PiHarnessStatus.ps1',
            'tooling/pi/Install-AgentSwitchboardPiSystem.ps1',
            'tooling/pi/Invoke-AgentSwitchboardPiChild.ps1',
            'tooling/pi/hooks/Invoke-PiHarnessPreCommit.ps1',
            'tests/Test-PiWorkstationPrereqsContracts.ps1',
            'tooling/profiles/windows/Setup-TechnicianAgentSwitchboard.ps1'
          )) {
            $tokens = $null
            $errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile($path, [ref]$tokens, [ref]$errors)
            foreach ($error in @($errors)) { $failures.Add("$path`: $($error.Message)") }
          }
          if ($failures.Count -gt 0) {
            $failures | ForEach-Object { Write-Error $_ }
            exit 1
          }
      - name: Run Pi workstation npm compatibility report-only path
        shell: pwsh
        run: ./tooling/pi/Test-PiWorkstationPrereqs.ps1 -NoNetwork -NoWrite -AllowUnready
      - name: Inspect system Pi bootstrap without mutation
        shell: pwsh
        run: ./tooling/pi/Install-AgentSwitchboardPiSystem.ps1 -Mode Inspect -RootPath .
      - name: Run executable Pi workstation prerequisite contracts
        shell: pwsh
        run: ./tests/Test-PiWorkstationPrereqsContracts.ps1
      - name: Run dependency-free Pi harness contracts
        shell: pwsh
        run: python tests/test_pi_harness_contracts.py
      - name: Run dependency-free Pi system and child contracts
        shell: pwsh
        run: python -m unittest tests.test_pi_system_bootstrap -v
      - name: Run Pi completeness contract
        shell: pwsh
        run: ./scripts/Test-PiHarnessCompleteness.ps1
      - name: Run Pi read-only status
        shell: pwsh
        run: ./tooling/pi/Get-PiHarnessStatus.ps1 -NoWrite
      - name: Run canonical documentation contract
        shell: pwsh
        run: ./scripts/Test-AgentDocumentationContract.ps1
      - name: Verify PR-range diff hygiene and clean checkout
        if: github.event_name == 'pull_request'
        shell: pwsh
        run: |
          git diff --check "origin/$env:GITHUB_BASE_REF...HEAD"
          if (git status --short) { throw 'Checkout is dirty after validation.' }
      - name: Verify push-range diff hygiene and clean checkout
        if: github.event_name != 'pull_request'
        shell: pwsh
        run: |
          git diff --check HEAD^..HEAD
          if (git status --short) { throw 'Checkout is dirty after validation.' }
